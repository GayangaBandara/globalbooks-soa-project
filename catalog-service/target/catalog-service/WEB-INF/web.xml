<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">
    
    <listener>
        <listener-class>com.sun.xml.ws.transport.http.servlet.WSServletContextListener</listener-class>
    </listener>
    
    <servlet>
        <servlet-name>CatalogService</servlet-name>
        <servlet-class>com.sun.xml.ws.transport.http.servlet.WSServlet</servlet-class>
    </servlet>
    
    <servlet-mapping>
        <servlet-name>CatalogService</servlet-name>
        <url-pattern>/catalog</url-pattern>
    </servlet-mapping>

    <!-- Configure MIME types -->
    <mime-mapping>
        <extension>wsdl</extension>
        <mime-type>text/xml</mime-type>
    </mime-mapping>
    <mime-mapping>
        <extension>xsd</extension>
        <mime-type>text/xml</mime-type>
    </mime-mapping>

    <!-- Configure WSDL stylesheet -->
    <context-param>
        <param-name>catalog.style</param-name>
        <param-value>/catalog-style.xsl</param-value>
    </context-param>

    <!-- Disable directory listing -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Restrict Directory Access</web-resource-name>
            <url-pattern>/*</url-pattern>
        </web-resource-collection>
        <auth-constraint/>
    </security-constraint>

    <!-- Redirect root to service -->
    <welcome-file-list>
        <welcome-file>catalog</welcome-file>
    </welcome-file-list>
</web-app>